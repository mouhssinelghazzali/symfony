{% extends 'base.html.twig' %}

{% block title %}Optionn{% endblock %}

{% block body %}
    <h1>Optionn</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ optionn.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ optionn.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('optionn_index') }}">back to list</a>

    <a href="{{ path('optionn_edit', {'id': optionn.id}) }}">edit</a>

    {{ include('optionn/_delete_form.html.twig') }}
{% endblock %}
