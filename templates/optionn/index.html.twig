{% extends 'base.html.twig' %}

{% block title %}Optionn index{% endblock %}

{% block body %}
    <h1>Optionn index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for optionn in optionns %}
            <tr>
                <td>{{ optionn.id }}</td>
                <td>{{ optionn.name }}</td>
                <td>
                    <a href="{{ path('optionn_show', {'id': optionn.id}) }}">show</a>
                    <a href="{{ path('optionn_edit', {'id': optionn.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('optionn_new') }}">Create new</a>
{% endblock %}
